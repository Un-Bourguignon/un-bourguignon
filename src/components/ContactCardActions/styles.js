const styles = (theme) => ({
  ContactCardActions: {
    padding: 8,
    display: "flex",
    justifyContent: "space-between",
  },
  gitHubButton: {
    marginRight: theme.spacing(2),
  },
});

export default styles;
